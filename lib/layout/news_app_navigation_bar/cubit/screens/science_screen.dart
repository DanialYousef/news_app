import 'package:flutter/material.dart';

class SciensScreen extends StatelessWidget {
  const SciensScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Science Screen'));
  }
}
